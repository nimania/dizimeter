import Link from"next/link";import{ArrowRight}from"lucide-react";import{latestRecaps}from"@/lib/demo-data";
export const metadata={title:"ری‌کپ قسمت‌ها"};
export default function Page(){return <main dir="rtl" className="inner-page"><section className="page-hero"><div className="shell"><Link href="/" className="back"><ArrowRight size={16}/>صفحه اصلی</Link><p>قصه تا اینجا</p><h1>تازه‌ترین ری‌کپ‌ها</h1><span>روایت منظم اتفاقات هر قسمت، مناسب کسی که قسمت قبل را ندیده یا جزئیاتش را فراموش کرده است.</span></div></section><section className="shell simple-list">{latestRecaps.map((x,i)=><Link href={`/dizi/${x.slug}`} className="simple-card" key={x.slug}><b className="rank">{i+1}</b><div><h2>{x.title}</h2><p>{x.summary}</p></div><strong className="simple-score">قسمت {x.episode}</strong></Link>)}</section></main>}
