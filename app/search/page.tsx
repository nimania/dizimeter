import Link from"next/link";import{ArrowRight}from"lucide-react";import{series}from"@/lib/demo-data";
export const metadata={title:"جست‌وجو"};
export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const{q=""}=await searchParams;const n=q.trim().toLocaleLowerCase("fa");const rows=n?series.filter(x=>`${x.titleFa} ${x.titleTr} ${x.network}`.toLocaleLowerCase("fa").includes(n)):[];return <main dir="rtl" className="inner-page"><section className="page-hero"><div className="shell"><Link href="/" className="back"><ArrowRight size={16}/>صفحه اصلی</Link><p>جست‌وجو</p><h1>{q?`نتیجه برای «${q}»`:"نام سریال را جست‌وجو کنید"}</h1><span>{q?`${rows.length} نتیجه در نسخه نمایشی پیدا شد.`:"از کادر جست‌وجوی صفحه اصلی استفاده کنید."}</span></div></section><section className="shell simple-list">{rows.map(x=><Link href={`/dizi/${x.slug}`} className="simple-card" key={x.slug}><b className="rank">{x.monogram}</b><div><h2>{x.titleFa}</h2><p>{x.titleTr} · {x.network}</p></div><strong className="simple-score">{x.total.toFixed(2)}</strong></Link>)}</section></main>}
